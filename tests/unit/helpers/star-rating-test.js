import { starRating } from 'jsreview4/helpers/star-rating';
import { module, test } from 'qunit';

module('Unit | Helper | star rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = starRating([42]);
  assert.ok(result);
});
